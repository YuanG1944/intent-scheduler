# Scheduler Skills

Reusable skills for intent-scheduler workflows.

## Available skills

- `intent-scheduler` (name: `intent-scheduler-minimal`)
  - Minimal-input task creation
  - Safe defaults for retries/timezone/adapter
  - Cross-integrator mapping guidance (`opencode`, `claude_code`, `codex_cli`)
